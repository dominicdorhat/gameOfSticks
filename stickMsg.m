function stickMsg(s)
     if s == 1
        disp('There is only 1 stick left on the table.');
     else
        fprintf('There are %d sticks on the table. \n', s);                                                                        
      end
end